extends base/layout.pug

block mainContent
  +breadcrumb()
  .container.py-3
    if page.title
      h1= page.title.split('/').pop().toUpperCase()
    else
      h1= 'Archivo'
    hr
    +post_list()
    +paginator_indexed()
